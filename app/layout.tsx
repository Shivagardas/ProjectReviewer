import './globals.css';
export const metadata={title:'PROVE — Engineering Discovery',description:'Discover engineering that matters.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}